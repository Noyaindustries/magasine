import { ReaderSpacePage } from "@/components/reader/ReaderSpacePage";

export const metadata = {
  title: "My reader space",
  description:
    "Your personal Global South Watch reader space — saved articles, reading history, and newsletter preferences.",
};

export default function ProfilePage() {
  return <ReaderSpacePage />;
}
